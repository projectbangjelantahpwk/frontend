---
interface Props {
	title: string;
	// Tambahkan prop ini untuk mengontrol state login (default: true untuk testing)
	isLoggedIn?: boolean; 
}

const { title, isLoggedIn = true } = Astro.props;
---

<!DOCTYPE html>
<html lang="id">
<head>
    <meta charset="utf-8">
    <meta content="width=device-width, initial-scale=1.0" name="viewport">
    <title>{title}</title>
    
    <script is:inline src="https://cdn.tailwindcss.com?plugins=forms,container-queries"></script>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&display=swap" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css2?family=Material+Symbols+Outlined:wght,FILL@100..700,0..1&display=swap" rel="stylesheet">
    
    <script is:inline id="tailwind-config">
        tailwind.config = {
            darkMode: "class",
            theme: {
                extend: {
                    "colors": {
                        "surface-tint": "#1b6d24",
                        "on-tertiary-fixed": "#311300",
                        "tertiary-fixed": "#ffdcc6",
                        "secondary-fixed": "#acf4a4",
                        "outline": "#707a6c",
                        "secondary-container": "#acf4a4",
                        "secondary": "#2a6b2c",
                        "surface-container-lowest": "#ffffff",
                        "inverse-on-surface": "#f2f0f0",
                        "surface-container": "#efeded",
                        "primary-container": "#2e7d32",
                        "on-secondary-fixed": "#002203",
                        "on-secondary-container": "#307231",
                        "background": "#fbf9f9",
                        "tertiary": "#884200",
                        "error-container": "#ffdad6",
                        "error": "#ba1a1a",
                        "primary": "#0d631b",
                        "on-tertiary": "#ffffff",
                        "surface-container-high": "#e9e8e7",
                        "on-primary-fixed-variant": "#005312",
                        "on-surface-variant": "#40493d",
                        "surface": "#fbf9f9",
                        "on-surface": "#1b1c1c",
                        "on-tertiary-container": "#ffeee6",
                        "tertiary-fixed-dim": "#ffb786",
                        "on-secondary": "#ffffff",
                        "surface-bright": "#fbf9f9",
                        "on-primary-container": "#cbffc2",
                        "surface-dim": "#dbdad9",
                        "surface-container-highest": "#e3e2e2",
                        "on-tertiary-fixed-variant": "#723600",
                        "inverse-primary": "#88d982",
                        "tertiary-container": "#ad5600",
                        "on-primary": "#ffffff",
                        "secondary-fixed-dim": "#91d78a",
                        "on-secondary-fixed-variant": "#0c5216",
                        "outline-variant": "#bfcaba",
                        "primary-fixed-dim": "#88d982",
                        "surface-container-low": "#f5f3f3",
                        "on-background": "#1b1c1c",
                        "surface-variant": "#e3e2e2",
                        "on-error-container": "#93000a",
                        "on-error": "#ffffff",
                        "inverse-surface": "#303031",
                        "primary-fixed": "#a3f69c",
                        "on-primary-fixed": "#002204"
                    },
                    "borderRadius": {
                        "DEFAULT": "0.125rem",
                        "lg": "0.25rem",
                        "xl": "0.5rem",
                        "full": "0.75rem"
                    },
                    "spacing": {
                        "md": "16px",
                        "lg": "24px",
                        "xs": "4px",
                        "container-margin": "16px",
                        "gutter": "12px",
                        "base": "8px",
                        "xl": "32px",
                        "touch-target": "48px",
                        "sm": "8px",
                        "safe": "env(safe-area-inset-bottom)"
                    },
                    "fontFamily": {
                        "body-lg": ["Inter", "sans-serif"],
                        "label-sm": ["Inter", "sans-serif"],
                        "label-lg": ["Inter", "sans-serif"],
                        "headline-lg": ["Inter", "sans-serif"],
                        "display-data": ["Inter", "sans-serif"],
                        "headline-md": ["Inter", "sans-serif"],
                        "body-md": ["Inter", "sans-serif"],
                        "headline-lg-mobile": ["Inter", "sans-serif"]
                    }
                }
            }
        }
    </script>
    <style is:global>
        .material-symbols-outlined {
            font-variation-settings: 'FILL' 0, 'wght' 400, 'GRAD' 0, 'opsz' 24;
        }
        .material-symbols-outlined[data-weight="fill"] {
            font-variation-settings: 'FILL' 1;
        }
        
        /* Animasi Transisi Sidebar & Overlay */
        .sidebar {
            transform: translateX(-100%);
            transition: transform 0.3s cubic-bezier(0.4, 0, 0.2, 1);
        }

        .sidebar.active {
            transform: translateX(0);
        }

        .sidebar-overlay {
            opacity: 0;
            visibility: hidden;
            transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
        }

        .sidebar-overlay.active {
            opacity: 1;
            visibility: visible;
        }
    </style>
</head>
<body class="bg-background text-on-background min-h-screen font-body-lg text-body-lg overflow-x-hidden">
    
    <header class="fixed top-0 left-0 w-full z-40 flex justify-between items-center px-4 md:px-lg h-16 bg-surface-container-lowest border-b border-outline-variant shadow-sm">
        
        <div class="flex items-center gap-2 md:gap-4">
            <button id="hamburger-btn" class="w-10 h-10 flex items-center justify-center text-on-surface-variant hover:bg-surface-container-low transition-colors rounded-full focus:outline-none">
                <span class="material-symbols-outlined text-[24px]">menu</span>
            </button>
            
            <a href="/" class="flex items-center gap-2 group text-decoration-none">
                <div class="w-8 h-8 rounded-lg bg-gradient-to-br from-primary to-surface-tint flex items-center justify-center text-on-primary shadow-sm group-hover:shadow transition-all">
                    <span class="material-symbols-outlined text-[20px]">eco</span>
                </div>
                <div class="flex items-baseline">
                    <h1 class="text-xl md:text-2xl font-bold text-on-surface tracking-tight">BangJelantah</h1>
                    <span class="hidden sm:inline-block ml-1.5 text-sm font-medium text-on-surface-variant uppercase tracking-widest">Workspace</span>
                </div>
            </a>
        </div>

        <div class="flex items-center gap-1 md:gap-2">
            <div id="auth-logged-out" class="flex items-center gap-2">
                <a href="/login" class="hidden sm:flex items-center px-4 py-2 text-sm font-semibold text-primary hover:bg-surface-container-low rounded-full transition-colors">
                    Masuk
                </a>
            </div>

            <div id="auth-logged-in" class="hidden items-center gap-1 md:gap-2">
                <button class="w-10 h-10 hidden sm:flex items-center justify-center text-on-surface-variant hover:bg-surface-container-low transition-colors rounded-full">
                    <span class="material-symbols-outlined">notifications</span>
                </button>
                <button class="w-10 h-10 hidden sm:flex items-center justify-center text-on-surface-variant hover:bg-surface-container-low transition-colors rounded-full">
                    <span class="material-symbols-outlined">settings</span>
                </button>
                <div class="ml-2 w-9 h-9 rounded-full overflow-hidden border-2 border-primary-container cursor-pointer hover:opacity-90 transition-opacity">
                    <img id="user-avatar" alt="User Avatar" class="w-full h-full object-cover" src="https://lh3.googleusercontent.com/aida-public/AB6AXuDb2mg6i0Q9z7vO0cZ9sjq-i1g8ob7hoHmXFluf-CaK74dOBnCEKHXIjmKniApFjfDKuffHA6xlUdUFl4mb_hTPCRWK0r5iMFTA6tGNJTaUMwCxrflyXotwePcLtD-LmS3Zd2IO0L0leRQAGz9aFB-5G3AIfOGVLN9md0sJpq_PkyDsTzZtv3RVJ9CkJLQ6N6RfQ-4aOlrVDrti1NJdM8dJOS6tWlhWmEF_0Tp3HNuhOWem5JZL0XJL6kmSrsLU1XWRjyfKFkV1QhQO">
                </div>
            </div>
        </div>
    </header>

    <div class="relative">
        <div id="sidebar-overlay" class="sidebar-overlay fixed inset-0 bg-on-surface/40 backdrop-blur-sm z-[50]"></div>

        <nav id="sidebar" class="sidebar fixed left-0 top-0 w-[280px] h-screen z-[60] bg-surface-container-lowest border-r border-outline-variant shadow-2xl flex flex-col p-4 overflow-y-auto">
            
            <div class="flex items-center justify-between mb-6 pt-2 px-2">
                <div class="flex items-center gap-2">
                    <div class="w-8 h-8 rounded-lg bg-primary-container text-on-primary-container flex items-center justify-center">
                        <span class="material-symbols-outlined text-[18px]">eco</span>
                    </div>
                    <span class="font-bold text-lg text-primary tracking-tight">Eco-Logistics</span>
                </div>
                <button id="close-sidebar" class="w-8 h-8 flex items-center justify-center text-on-surface-variant hover:bg-surface-container-low transition-colors rounded-full">
                    <span class="material-symbols-outlined text-[20px]">close</span>
                </button>
            </div>

            <div class="flex-1 flex flex-col gap-1">
                <p class="px-3 py-2 text-xs font-semibold text-on-surface-variant uppercase tracking-wider mb-1">Menu Utama</p>
                
                <a class="nav-link flex items-center gap-3 px-4 py-3 rounded-xl active:scale-[0.98] transition-all" href="/" data-nav>
                    <span class="material-symbols-outlined text-[22px]" data-weight="fill">dashboard</span>
                    <span class="font-medium text-sm">Dashboard</span>
                </a>
                <a class="nav-link flex items-center gap-3 px-4 py-3 rounded-xl active:scale-[0.98] transition-all" href="/kemitraan" data-nav>
                    <span class="material-symbols-outlined text-[22px]">handshake</span>
                    <span class="font-medium text-sm">Kemitraan</span>
                </a>
                <a class="nav-link flex items-center gap-3 px-4 py-3 rounded-xl active:scale-[0.98] transition-all" href="/operasional" data-nav>
                    <span class="material-symbols-outlined text-[22px]">local_shipping</span>
                    <span class="font-medium text-sm">Operasional</span>
                </a>
                <a class="nav-link flex items-center gap-3 px-4 py-3 rounded-xl active:scale-[0.98] transition-all" href="/gudang" data-nav>
                    <span class="material-symbols-outlined text-[22px]">inventory_2</span>
                    <span class="font-medium text-sm">Gudang</span>
                </a>
                
                <p class="px-3 py-2 text-xs font-semibold text-on-surface-variant uppercase tracking-wider mt-4 mb-1">Manajemen</p>
                
                <a class="nav-link flex items-center gap-3 px-4 py-3 rounded-xl active:scale-[0.98] transition-all" href="/karyawan" data-nav>
                    <span class="material-symbols-outlined text-[22px]">people</span>
                    <span class="font-medium text-sm">Karyawan</span>
                </a>
                <a class="nav-link flex items-center gap-3 px-4 py-3 rounded-xl active:scale-[0.98] transition-all" href="/keuangan" data-nav>
                    <span class="material-symbols-outlined text-[22px]">payments</span>
                    <span class="font-medium text-sm">Keuangan</span>
                </a>
                <a class="nav-link flex items-center gap-3 px-4 py-3 rounded-xl active:scale-[0.98] transition-all" href="/periklanan" data-nav>
                    <span class="material-symbols-outlined text-[22px]">campaign</span>
                    <span class="font-medium text-sm">Periklanan</span>
                </a>
                <a class="nav-link flex items-center gap-3 px-4 py-3 rounded-xl active:scale-[0.98] transition-all" href="/hak-akses" data-nav>
                    <span class="material-symbols-outlined text-[22px]">manage_accounts</span>
                    <span class="font-medium text-sm">Hak Akses</span>
                </a>
            </div>
            <div id="sidebar-footer" class="hidden mt-6 pt-4 border-t border-outline-variant">
                <button id="btn-logout" class="w-full flex items-center justify-center gap-2 bg-error-container text-on-error-container px-4 py-3 rounded-xl font-medium text-sm hover:opacity-90 transition-opacity active:scale-[0.98]">
                    <span class="material-symbols-outlined text-[20px]">logout</span>
                    <span>Akhiri Sesi</span>
                </button>
            </div>
        </nav>
    </div>

    <main class="w-full min-h-screen pt-20 pb-8 px-4 md:px-lg overflow-x-hidden">
        <slot />
    </main>

    <script is:inline>
        const hamburgerBtn = document.getElementById('hamburger-btn');
        const sidebar = document.getElementById('sidebar');
        const closeBtn = document.getElementById('close-sidebar');
        const overlay = document.getElementById('sidebar-overlay');
        const navLinks = document.querySelectorAll('[data-nav]');
        const currentPath = window.location.pathname;

        // KONDISI: Membuka aside HANYA boleh di rute '/' (Dashboard utama)
        if (currentPath !== '/') {
            // Sembunyikan tombol hamburger secara total di halaman lain
            if (hamburgerBtn) {
                hamburgerBtn.style.display = 'none';
            }
        }

        function openSidebar() {
            // Memastikan aksi buka hanya berjalan jika berada tepat di rute '/'
            if (currentPath === '/') {
                sidebar.classList.add('active');
                overlay.classList.add('active');
                document.body.style.overflow = 'hidden';
            }
        }

        function closeSidebar() {
            sidebar.classList.remove('active');
            overlay.classList.remove('active');
            document.body.style.overflow = '';
        }

        hamburgerBtn?.addEventListener('click', openSidebar);
        closeBtn?.addEventListener('click', closeSidebar);
        overlay?.addEventListener('click', closeSidebar);

        // Menentukan Menu Navigasi Aktif (Styling)
        function setActiveNav() {
            navLinks.forEach(link => {
                link.classList.remove('bg-primary-container', 'text-on-primary-container', 'font-bold');
                link.classList.add('text-on-surface-variant', 'hover:bg-surface-container-low');
                
                const href = link.getAttribute('href');
                if ((currentPath === '/' && href === '/') || (currentPath !== '/' && currentPath === href)) {
                    link.classList.remove('text-on-surface-variant', 'hover:bg-surface-container-low');
                    link.classList.add('bg-primary-container', 'text-on-primary-container', 'font-bold');
                }
            });
        }
        setActiveNav();

        // INTEGRASI DENGAN STATUS LOGIN SUPABASE
        async function initAuthCheck() {
            if (window.supabase) {
                // Ambil data session aktif saat halaman dimuat
                const { data: { session } } = await window.supabase.auth.getSession();
                renderAuthUI(session);

                // Dengarkan perubahan status autentikasi secara realtime (Login/Logout)
                window.supabase.auth.onAuthStateChange((_event, session) => {
                    renderAuthUI(session);
                });
            }
        }

        function renderAuthUI(session) {
            const loggedOutView = document.getElementById('auth-logged-out');
            const loggedInView = document.getElementById('auth-logged-in');
            const sidebarFooter = document.getElementById('sidebar-footer');

            if (session) {
                // Jika sesi ditemukan (User Terautentikasi)
                loggedOutView?.classList.add('hidden');
                loggedInView?.classList.remove('hidden');
                sidebarFooter?.classList.remove('hidden');

                // Sinkronisasi foto profil dari metadata jika tersedia
                const avatarImg = document.getElementById('user-avatar');
                if (avatarImg && session.user.user_metadata?.avatar_url) {
                    avatarImg.src = session.user.user_metadata.avatar_url;
                }
            } else {
                // Jika tidak ada sesi (Belum Login / Keluar)
                loggedOutView?.classList.remove('hidden');
                loggedInView?.classList.add('hidden');
                sidebarFooter?.classList.add('hidden');
            }
        }

        // Eksekusi fungsi pengecekan
        initAuthCheck();

        // Event listener untuk tombol logout harian
        document.getElementById('btn-logout')?.addEventListener('click', async () => {
            if (window.supabase) {
                await window.supabase.auth.signOut();
                window.location.href = '/login';
            }
        });
    </script>
</body>
</html>